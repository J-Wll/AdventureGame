#pragma once

class Location {
	string name;
	string type;
	// list of monsters at that location, like 3 per?
	// also a list of items/equipment (objects) 
	// monsters and or equip could be randomly generated from a bigger list
};